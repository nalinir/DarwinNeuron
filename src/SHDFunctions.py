


# TO DO:

def generate_and_save_shd(save_dir="data/shd"):    
    # Format will be TensorDataset that's saved (check to see if this is the case)
    # Then make a metadata file with filepaths
    # We might just be able to simplify this to the link since no configs?
    # BUT will be helpful for the postprocessing we do (if choose to do so)..
    
    
    
    # It's actually a dataloader instead of a tensordataset, we can revisit this later
    return filepath

def generate_randman_problem(nb_hidden=40, loss_fn = 'cross_entropy', db_path='data/landscape-analysis.db'):
    # Create SNN model to calculate the number of parameters required
    # We will eventually use the other models, but use theirs to start

    # Can probably use the same function just need to pass things well

# Keep the same sampling strategy
## Eventually we will probably not want a random walk





    





